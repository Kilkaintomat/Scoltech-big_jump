# Необходимые повторные запуски

Оценки ресурсов — по фактическому времени уже выполненных джобов на Zhores.

## Категория A. Пересчёт из сохранённого — GPU не нужен

| # | Что | Почему | Вход | Стоимость | Требуется доработка |
|---|---|---|---|---|---|
| A1 | Пересчитать все выводы P4 через `ξ = max(moment, 0)` | P0-2: `analyse_p4` называет Хилла `gamma`; порядковый параметр статьи не считался | `results/full/grokking*/p4_grokking_seed*.csv` (целы) | минуты, CPU | **да** — исправить `analyse_p4`, отделить `gamma` от `xi` |
| A2 | Перерисовать Figure 1 и перезаписать дайджесты | P0-4: обе фигуры не совпадают с манифестом | `results/simulations/kesten/figure1_metrics.json` (цел) | секунды, CPU | нет — `onebigjump figure` существует |
| A3 | Пересчитать Kesten при сетке `k` и сообщить чувствительность | P1-1, P1-2: селектор попал в патологический минимум; `k/n` скачет 2–20 % | детерминированная симуляция по seed | ~10 мин, CPU | **да** — добавить отчёт по сетке `k` в артефакт |
| A4 | Пересчитать долю отказов с `Z_{t*} > τ` отдельно от подгонки P3 | §10.2 | `kesten-p0.05.json` | минуты, CPU | **да** — вынести долю в таблицу |

## Категория B. Требуется новая генерация

| # | Что | Почему | Стоимость | Требуется доработка |
|---|---|---|---|---|
| B1 | Пересэмплировать 3616 проофов с сохранением token IDs и сырого completion | P1-6, P1-7: id не сохранены (`n_prompt_tokens = 0`), informal reasoning утрачен безвозвратно | **~4 мин на A100** (vLLM eager, факт: 141 с на генерацию + загрузка весов) | **да** — писать `prompt_token_ids`, `completion_token_ids`, `raw_completion` в `Sample` |

## Категория C. Требуется новое обучение

| # | Что | Почему | Стоимость |
|---|---|---|---|
| C1 | Ничего для P4 в текущей постановке | Отслеживаемая величина исправляется пересчётом (A1), переобучение не нужно | — |
| C2 | Проверка (A): один чекпоинт, одинаковые входы, разные устройства | P2-8: вывод об «MPS-артефакте» сделан без неё | нужны сохранённые веса чекпоинтов, **которых нет** — потребует переобучения одного seed на каждом бэкенде, ~9 мин CUDA + ~28 мин CPU + MPS локально |

## Категория D. Блокирующая инфраструктура

| # | Что | Почему | Стоимость |
|---|---|---|---|
| D1 | Собрать Mathlib и REPL на кластере | P2-7: `.lake` и `repl` отсутствуют; без них ни одна верификация невозможна | 1–3 ч на `ais-htc`, сеть; **`release.lean-lang.org` недоступен с Zhores** — тулчейн ставится с GitHub (`scripts/install_lean_toolchain.sh`) |
| D2 | Починить провенанс на GPU-нодах | P0-5: git 1.8.3.1 без `-C` | минуты — заменить `git -C "$REPO"` на `cd "$REPO" && git …` |
| D3 | Развести имена в `paper_outputs` по имени прогона | P0-1: нулевой прогон перезаписал P4 | минуты |
| D4 | Манифест на каждый seed, а не на каталог | P0-6: у seeds 0,1 манифеста нет | минуты |
| D5 | Восстановить перезаписанные `paper_outputs/*p4*` из git | P0-1 | секунды, `git checkout` |

## Порядок

D2 → D3 → D4 (провенанс, иначе всё последующее снова потеряет происхождение)
→ A1, A2 (дешёвые пересчёты, закрывают P0-2 и P0-4)
→ D1 (долгий, запускать параллельно с A)
→ B1 (после D1, чтобы верификация и извлечение шли по одним данным)
→ A3, A4
